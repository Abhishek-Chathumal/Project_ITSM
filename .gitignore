node_modules/
dist/
build/
.turbo/
*.tsbuildinfo

# Prisma 7 generates the client into the source tree (the generator's `output` is
# mandatory and no longer points at node_modules). It is build output, not source:
# `npx prisma generate` recreates it, and CI does exactly that before every build.
apps/api/src/generated/

# env
# Ignore every .env variant, then re-admit the committed template. The wildcard matters:
# `.env` and `.env.local` alone leave `.env.bak`, `.env.save`, `.env.prod` and whatever an
# editor or a careless backup drops next to them fully trackable, and `git add -A` will
# happily stage a real secrets file under any of those names.
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/*
!.vscode/extensions.json
.idea/

# Prisma
apps/api/prisma/dev.db

# Vite
apps/web/dist/

# Docker
infra/docker/*.pid
